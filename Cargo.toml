[workspace]
members = [
    "kernel",
    "init",
    "klib",
    "memintrinsics",
]

[profile.release]
opt-level = "s"
debug = 0
overflow-checks = false
lto = true
codegen-units = 1
# panic = "abort"
