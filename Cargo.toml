[workspace]
members = [
    "kernel",
    "init",
    "klib",
    "syscall",
    "ipc",
    "malloc",
    "memintrinsics",
]

[profile.release]
opt-level = "s"
debug = 0
overflow-checks = false
lto = true
codegen-units = 1
# panic = "abort"
